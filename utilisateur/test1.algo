algorithme Test1

constante:
	numero : entier <-- 9
	chiffre: reel <-- 52.4
	symbole: caractere <-- '$'
	phrase : chaine <-- "rien de nouveau sous le soleil"
	verite <-- vrai
variable:
	vBool  :booleen
	cpt    :entier
	str    :chaine
debut
	cpt <-- 10 / 3
	vBool <-- vrai
	str <-- enChaine(6)

	/*
	écrire("impossible")
	*/

	si verite ou faux alors
		ecrire("écrire" © " biensur")
	sinon
		ecrire("On est pas censé etre ici ")
	fsi

	si verite et faux alors
		ecrire("écrire")
	sinon
		ecrire(verite)
	fsi

	si verite xou faux alors
		écrire("les test simple fonctionne")
	sinon
		ecrire("On est pas censé etre ici ")
	fsi

	si vBool alors
		si verité alors
			si faux alors
				ecrire("les test imbriqué fonctionne")
			fsi
		fsi
	fsi

	lire(numero)

/*
	un commentaire multiligne
*/

fin